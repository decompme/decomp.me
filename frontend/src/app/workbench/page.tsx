import Workbench from "./Workbench";

export const metadata = {
    title: "Scratch workbench",
};

export const dynamic = "force-dynamic";

export default function WorkbenchPage() {
    return <Workbench />;
}
