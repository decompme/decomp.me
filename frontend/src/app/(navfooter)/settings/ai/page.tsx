import AiSettings from "./AiSettings";

export const metadata = {
    title: "AI settings",
};

export default function Page() {
    return (
        <>
            <AiSettings />
        </>
    );
}
